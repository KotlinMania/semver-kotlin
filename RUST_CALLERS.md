# Rust callers — `semver-kotlin`

> Generated by `scaffold/generate_caller_reports.py` from `workspace_dep_graph.json` (Cargo-aware dep graph). Do not hand-edit. Re-run after the graph is rebuilt.

## What this file is

For every Rust crate this repo owns (under `tmp/`), this file lists every **cross-repo** workspace caller: every `use` statement in another `*-kotlin` repo's Rust source that imports a symbol from one of this repo's crates. Symbols are grouped per-crate; codex-kotlin sites are surfaced first per symbol (codex is the priority consumer driving the port queue).

**Granularity:** import-level (each row is one `use` line). Call-site detail — where the imported name is actually invoked, implemented, pattern-matched, or used as a generic bound inside the caller body — requires a per-symbol grep across the listed caller files. The graph tells you exactly which files to grep.

**Cross-repo only.** Imports between crates inside this same repo are omitted — those are intra-repo refactoring concerns, not the cross-project porting concern this file exists to surface.

**Cargo-resolved.** Counts here honor `package = "..."` renames, `workspace = true` inheritance, and `[target.<cfg>.dependencies]`. Stdlib (`std`, `core`, `alloc`) and intra-crate sub-module references are excluded.

## Crates in this repo

| package | module | inbound use sites | distinct caller crates | distinct caller repos | codex sites |
|---|---|---:|---:|---:|---:|
| `semver` | `semver` | 14 | 5 | 5 | 2 |

## `semver`

### Inbound symbols (3 distinct, 14 total `use` sites)

Sorted by codex demand, then total demand. The first symbols are the priority queue for porting `semver`.

#### `semver::Version` — 11 site(s) (2 codex, 9 other, 4 caller crates)

- ✅ `codex-kotlin/codex-ollama` `src/client.rs:4` — `use semver::Version`
- ✅ `codex-kotlin/codex-ollama` `src/lib.rs:13` — `use semver::Version`
- `libwebrtc-kotlin/livekit` `src/room/participant/local_participant.rs:48` — `use semver::Version`
- `runfiles-kotlin/cargo-bazel` `src/api/lockfile.rs:122` — `use semver::Version`
- `runfiles-kotlin/cargo-bazel` `src/context.rs:295` — `use semver::Version`
- `runfiles-kotlin/cargo-bazel` `src/metadata/cargo_bin.rs:11` — `use semver::Version`
- `runfiles-kotlin/cargo-bazel` `src/metadata/cargo_tree_resolver.rs:11` — `use semver::Version`
- `runfiles-kotlin/cargo-bazel` `src/metadata/dependency.rs:376` — `use semver::Version`
- `runfiles-kotlin/cargo-bazel` `src/metadata/metadata_annotation.rs:557` — `use semver::Version`
- `runfiles-kotlin/cargo-bazel` `src/splicing/crate_index_lookup.rs:67` — `use semver::Version`
- `ts-rs-kotlin/ts-rs` `tests/integration/semver.rs:3` — `use semver::Version`

#### `semver1::Version` — 2 site(s) (0 codex, 2 other, 1 caller crates)

- `schemars-kotlin/schemars` `src/json_schema_impls/semver1.rs:4` — `use semver1::Version`
- `schemars-kotlin/schemars` `tests/integration/semver.rs:2` — `use semver1::Version`

#### `semver::VersionReq` — 1 site(s) (0 codex, 1 other, 1 caller crates)

- `runfiles-kotlin/cargo-bazel` `src/config.rs:15` — `use semver::VersionReq`

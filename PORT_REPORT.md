# Port report — `semver-kotlin`

> Generated by `scaffold/generate_repo_reports.py` from `workspace_dep_graph.json` ⨯ `port_priority.json`. Do not hand-edit. Re-run after the workspace dep graph is rebuilt.

## Overview

- **1** member crate(s): `semver`
- **Codex-kotlin distance**: **hop 1** from codex-kotlin via cross-repo Rust deps
- **Inbound**: 14 `use` sites from 5 other repos
- **Outbound**: 6 `use` sites to 1 other repos
- **Unresolved external**: 0 published Cargo crates with no `*-kotlin/` port

## Member crates

| package | module | inbound `use` sites | outbound external repos | Jira | rank | status |
|---|---|---:|---:|---|---:|---|
| `semver` | `semver` | 14 | 1 | COD-126 | 38 | To Do |

## Inbound — what to port first

Every symbol this repo's crates need to expose, ranked by total workspace demand. **Codex-kotlin sites are listed first per symbol** because codex is the priority consumer driving the port queue. Anything not listed here is not currently `use`'d by any kotlinmania repo and can be deferred.

### Inbound caller summary (14 edges, 5 caller crates)

| caller repo | caller crate | use sites | codex consumer? |
|---|---|---:|---|
| `runfiles-kotlin` | `cargo-bazel` | 8 |  |
| `codex-kotlin` | `codex-ollama` | 2 | ✅ |
| `schemars-kotlin` | `schemars` | 2 |  |
| `libwebrtc-kotlin` | `livekit` | 1 |  |
| `ts-rs-kotlin` | `ts-rs` | 1 |  |

### Symbol-level demand (3 distinct symbols)

Sorted by codex demand, then total demand. Symbols with codex-kotlin sites are the priority queue. Each symbol entry below shows up to 12 call sites (codex first).

| symbol | total | codex | other | distinct callers |
|---|---:|---:|---:|---:|
| `semver::Version` | 11 | 2 | 9 | 4 |
| `semver1::Version` | 2 | 0 | 2 | 1 |
| `semver::VersionReq` | 1 | 0 | 1 | 1 |

#### Per-symbol detail (top 3 symbols)

##### `semver::Version` (11 sites, 2 codex, 9 other, 4 caller crates)

Codex call sites:

- `codex-kotlin/codex-ollama` `src/client.rs:4` — `use semver::Version`
- `codex-kotlin/codex-ollama` `src/lib.rs:13` — `use semver::Version`

Other workspace call sites:

- `libwebrtc-kotlin/livekit` `src/room/participant/local_participant.rs:48` — `use semver::Version`
- `runfiles-kotlin/cargo-bazel` `src/api/lockfile.rs:122` — `use semver::Version`
- `runfiles-kotlin/cargo-bazel` `src/context.rs:295` — `use semver::Version`
- `runfiles-kotlin/cargo-bazel` `src/metadata/cargo_bin.rs:11` — `use semver::Version`
- `runfiles-kotlin/cargo-bazel` `src/metadata/cargo_tree_resolver.rs:11` — `use semver::Version`
- `runfiles-kotlin/cargo-bazel` `src/metadata/dependency.rs:376` — `use semver::Version`
- `runfiles-kotlin/cargo-bazel` `src/metadata/metadata_annotation.rs:557` — `use semver::Version`
- `runfiles-kotlin/cargo-bazel` `src/splicing/crate_index_lookup.rs:67` — `use semver::Version`
- `ts-rs-kotlin/ts-rs` `tests/integration/semver.rs:3` — `use semver::Version`

##### `semver1::Version` (2 sites, 0 codex, 2 other, 1 caller crates)

Other workspace call sites:

- `schemars-kotlin/schemars` `src/json_schema_impls/semver1.rs:4` — `use semver1::Version`
- `schemars-kotlin/schemars` `tests/integration/semver.rs:2` — `use semver1::Version`

##### `semver::VersionReq` (1 sites, 0 codex, 1 other, 1 caller crates)

Other workspace call sites:

- `runfiles-kotlin/cargo-bazel` `src/config.rs:15` — `use semver::VersionReq`

## Outbound — what this repo depends on

Other kotlinmania repos this repo's Rust source `use`s. Symbol-level detail per target lives in **that target repo's PORT_REPORT.md inbound section** (no duplication). Sorted by Jira rank, then by `use`-site count.

| target repo | use sites | distinct symbols | target packages | Jira | rank | status |
|---|---:|---:|---|---|---:|---|
| `serde-kotlin` | 6 | 6 | `serde` | COD-128 | 1 | To Do |

## Unresolved external dependencies

Published Cargo crates this repo's Rust source `use`s but for which no `<crate>-kotlin/` repo exists. Each is either a missing port to file in Jira, or an intentional external dep (system library / FFI / build-only). If a port lives at a non-conventional repo name, add it to `PACKAGE_REPO_OVERRIDES` in `scaffold/build_workspace_dep_graph.py`.

_None — every external dep has a `*-kotlin/` port._
